import {StyleSheet} from 'react-native';
import {GRAY_200, GRAY_400} from '../../styles';

export const styles = StyleSheet.create({
  counter: {
    alignItems: 'center',
    backgroundColor: GRAY_400,
    borderRadius: 100,
  },
  text: {
    color: GRAY_200,
    paddingHorizontal: 8,
    paddingVertical: 2,
  },
});
